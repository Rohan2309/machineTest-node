<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || "Product System" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .flash-message {
      position: fixed;
      top: 15px;
      right: 15px;
      z-index: 9999;
      min-width: 250px;
      opacity: 0.95;
      border-radius: 8px;
      padding: 12px 18px;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
      <a class="navbar-brand" href="/">Product System</a>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a href="/" class="nav-link">Customer Portal</a></li>
        <li class="nav-item"><a href="/admin" class="nav-link">Admin Panel</a></li>
      </ul>
    </div>
  </nav>

  <!-- Flash Messages -->
  <% if (success) { %>
    <div class="alert alert-success flash-message"><%= success %></div>
  <% } %>
  <% if (error) { %>
    <div class="alert alert-danger flash-message"><%= error %></div>
  <% } %>

  <div class="container">
